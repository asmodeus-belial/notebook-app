<div class="container mx-auto p-4">
 <header class="flex items-center justify-between bg-white shadow-md p-2 rounded">
		<div class="flex">
				<input type="text" placeholder="Search..." class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
		</div>
		<div>
    	<%= link_to 'Back to Page', pages_path, class: 'bg-gray-300 text-gray-700 px-4 py-2 rounded hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-opacity-50 ml-2' %>
    </div>
  </header>
  <div class="max-w-sm mx-auto mt-4">
    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
      <div class="p-6">
        <h2 class="text-xl font-semibold text-gray-800"><%= @page.title%></h2>
        <p class="mt-2 text-gray-600"><%= @page.context%></p>
        <p class="mt-2 text-gray-600"><%= @page.emoji_category%></p>
        <p class="mt-2 text-gray-600"><%= @page.notebook.title%></p>
        <div class="mt-4">
          <%= link_to 'Edit Page', edit_page_path(@page), class: 'bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600' %>
          <%= link_to 'Delete', page_path(@page), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?" }, class: 'bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600' %>
        </div>
      </div>
    </div>
  </div>
</div>
